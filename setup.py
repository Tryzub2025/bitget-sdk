from setuptools import setup, find_packages

setup(
    name="bitget_sdk",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    description="Bitget SDK for Python",
    author="Bitget",
)
